@model PaginatedList<PawsitiveHealthHub.Models.MedRecords>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Medical Records</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<form asp-action="Index" method="get" class="d-flex align-items-center gap-2 mb-3">
    <input type="text" name="petSearch" value="@ViewData["CurrentPetSearch"]" class="form-control w-25" placeholder="Search pet..." />
    <input type="text" name="vetSearch" value="@ViewData["CurrentVetSearch"]" class="form-control w-25" placeholder="Search vet..." />
    <button type="submit" class="btn btn-primary">Search</button>
    <a asp-action="Index" class="btn btn-secondary">Clear</a>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-action="Index"
                   asp-route-sortOrder="@ViewData["PetSortParam"]"
                   asp-route-petSearch="@ViewData["CurrentPetSearch"]"
                   asp-route-vetSearch="@ViewData["CurrentVetSearch"]">
                    Pet
                    @if ((string)ViewData["CurrentSort"] == "pet_desc")
                    {
                        <span>↓</span>
                        ;
                    }
                    else if ((string)ViewData["CurrentSort"] == "pet_asc")
                    {

                        <span>↑</span>
                        ;
                    }
                </a>
            </th>
            <th>
                @Html.DisplayNameFor(model => model[0].Vet)
            </th>
            <th>
                @Html.DisplayNameFor(model => model[0].Diagnosis)
            </th>
            <th>
                @Html.DisplayNameFor(model => model[0].Treatment)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.Pet?.PetName
                </td>
                <td>
                    @("Dr. " + item.Vet?.LastName)
                </td>
                <td>
                    @item.Diagnosis
                </td>
                <td>
                    @item.Treatment
                </td>
                <td>
                    <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-id="@item.RecordID">Edit</a> |
                    <a class="btn btn-sm btn-secondary" asp-action="Details" asp-route-id="@item.RecordID">Details</a> |
                    <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@item.RecordID">Delete</a>
                </td>
            </tr>
        }
    </tbody>

</table>
@{
    var prevDisabled = !Model.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.HasNextPage ? "disabled" : "";
}

<nav>
    <ul class="pagination">
        <li class="page-item @prevDisabled">
            <a class="page-link" asp-action="Index"
               asp-route-pageNumber="@(Model.PageIndex - 1)"
               asp-route-sortOrder="@ViewData["CurrentSort"]"
               asp-route-petSearch="@ViewData["CurrentPetSearch"]"
               asp-route-vetSearch="@ViewData["CurrentVetSearch"]">
                Previous
            </a>
        </li>
        <li class="page-item disabled">
            <span class="page-link">
                Page @Model.PageIndex of @Model.TotalPages
            </span>
        </li>
        <li class="page-item @nextDisabled">
            <a class="page-link" asp-action="Index"
               asp-route-pageNumber="@(Model.PageIndex + 1)"
               asp-route-sortOrder="@ViewData["CurrentSort"]"
               asp-route-petSearch="@ViewData["CurrentPetSearch"]"
               asp-route-vetSearch="@ViewData["CurrentVetSearch"]">
                Next
            </a>
        </li>
    </ul>
</nav>